# Tarefa 2.2 — Tela de login com campos usuário + senha

## Status: ✅ Concluída

## O que foi feito

- `src/pages/Login.tsx` — tela de login com:
  - Campo de usuário e senha
  - Checkbox "Continuar conectado neste aparelho"
  - Logo/inicial do negócio + nome no topo
  - Botão de submit com loading state
  - Mensagem de erro
  - Feedback `active:scale-[0.975]` no botão
- `src/pages/Login.tsx` recebe `onTenantsFound` callback para o caso de múltiplos tenants
- `src/contexts/AuthContext.tsx` — `signIn()` que:
  - Chama `supabase.auth.signInWithPassword()`
  - Busca vínculos do usuário em `usuarios`
  - Se 1 tenant: seta user direto
  - Se múltiplos: retorna lista para o seletor
