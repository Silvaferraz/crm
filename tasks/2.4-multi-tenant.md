# Tarefa 2.4 — Fluxo multi-tenant

## Status: ✅ Concluída

## O que foi feito

- `src/pages/TenantSelect.tsx` — seletor de negócio com lista de rádio
- `src/pages/LoginPage.tsx` — gerencia o fluxo:
  - Step `login` → usuário digita credenciais
  - Step `select` → usuário escolhe o negócio
- `src/contexts/AuthContext.tsx`:
  - `signIn()` retorna `tenantsVinculados` se >1
  - Se 1 tenant, loga direto (sem seletor)
- `src/contexts/TenantContext.tsx` — guarda o tenant ativo
- Ao selecionar, grava cookie `tenant_slug` e redireciona para home
