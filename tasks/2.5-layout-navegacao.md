# Tarefa 2.5 — Layout autenticado (bottom-nav / sidebar)

## Status: ✅ Concluída

## O que foi feito

### Mobile (`BottomNav.tsx`)
- Barra fixa na parte inferior, translúcida (`backdrop-blur-lg`)
- Até 5 itens: Cobrar, Agenda, Clientes, Mensagens e Caixa (só admin)
- Item ativo marcado com traço no topo + cor (não só opacidade)

### Desktop (`Sidebar.tsx`)
- Barra lateral fixa de 240px a partir de 768px
- Logo/inicial + nome do negócio no topo
- Itens em formato de pastilha, ativo com fundo elevado
- Avatar + nome + papel + botão sair no rodapé
- Link para Ajustes (não na nav principal)
- Link para Painel da Plataforma (só super_admin)

### AppLayout
- `md:ml-60` para sidebar no desktop
- `pb-20` para bottom-nav no mobile
- Conteúdo centralizado com `max-w-[48rem]`
- Aplica cores do tenant como CSS variables via `useEffect`
