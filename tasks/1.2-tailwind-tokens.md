# Tarefa 1.2 — Configurar Tailwind CSS com tema escuro e tokens de cor

## Status: ✅ Concluída

## O que foi feito

- Tailwind configurado via plugin `@tailwindcss/vite`
- `src/styles/globals.css` criado com:
  - Bloco `@theme` com todos os tokens de cor da Seção 8.2: `marca`, `fundo`, `card`, `elevado`, `fosco`, `borda`, `borda-forte`, `suave`, `tenue`, `sucesso`, `sucesso-claro`, `perigo`, `perigo-claro`, `alerta`, `alerta-claro`
  - Cores do tenant aplicadas via variáveis CSS (`--tenant-primaria`, `--tenant-fundo`, `--tenant-card`) com fallback escuro
  - Fonte Inter configurada como padrão
  - `color-scheme: dark` global
  - `tabular-nums` via atributo `data-slot="tabular"`
  - `prefers-reduced-motion` respeitado
- `App.tsx` atualizado para usar classes Tailwind (`bg-fundo`, `text-marca`)
