# Tarefa 1.9 — Adicionar fonte Inter e tabular-nums

## Status: ✅ Concluída

## O que foi feito

Em `src/styles/globals.css`:
- Fonte `Inter` configurada como `--font-sans` no tema Tailwind, com fallback `system-ui, -apple-system, sans-serif`
- `tabular-nums` via `[data-slot="tabular"] { font-variant-numeric: tabular-nums }` — aplicar nos elementos que exibem valores numéricos (R$, contagens)
