# Tarefa 1.3 — Configurar projeto Supabase (banco, auth, storage)

## Status: ✅ Concluída

## O que foi feito

1. **Supabase CLI** encontrado via npx (v2.110.0)
2. Containers Docker existentes do projeto anterior parados
3. `supabase init` executado — criado `supabase/config.toml` com `project_id = "crm"`
4. `supabase start` executado — containers locais rodando:

| Serviço | Porta |
|---------|-------|
| Studio | 54323 |
| API (Kong) | 54321 |
| Postgres | 54322 |
| Auth (GoTrue) | interno |
| Storage | interno |
| Edge Runtime | interno |
| Mailpit | 54324 |

5. Migrations aplicadas com sucesso:
   - `00001_schema.sql` — schema completo (29 tabelas)
   - `00002_rls.sql` — RLS + funções `public.tenant_id()`, `public.is_super_admin()`, `public.tenant_policy()`
   - `00003_seed.sql` — dados fictícios de desenvolvimento

6. `.env.local` criado com credenciais locais do Supabase
