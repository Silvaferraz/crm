# Tarefa 2.9 — Logout e proteção de rotas

## Status: ✅ Concluída

## O que foi feito

- `src/routes/ProtectedRoute.tsx`:
  - Se `loading`, mostra spinner
  - Se `!user`, redireciona para `/login`
  - Se `user`, renderiza `<Outlet />`
- `src/routes/index.tsx`:
  - `/login` — rota pública (fora do layout)
  - Rotas protegidas dentro de `<ProtectedRoute>`
  - `*` → redireciona para `/`
- `src/contexts/AuthContext.tsx`:
  - `signOut()` — chama `supabase.auth.signOut()` + limpa user
  - Observa `onAuthStateChange` para detectar expiração de sessão
- `Sidebar.tsx`: botão de sair no rodapé (ícone de porta)
