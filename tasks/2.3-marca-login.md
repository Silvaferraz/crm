# Tarefa 2.3 — Marca do negócio no login (cookie/URL)

## Status: ✅ Concluída

## O que foi feito

- `src/lib/cookies.ts` — `getTenantCookie()` e `setTenantCookie()`
- `src/pages/LoginPage.tsx` — ao montar, lê cookie `tenant_slug` e busca dados do tenant no Supabase
- Login exibe logo + nome do tenant se resolvido, senão mostra identidade genérica "TrueBarbershop"
- Cookie de longa duração (1 ano), usado apenas para branding (nunca para autenticar)
